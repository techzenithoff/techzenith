$("ul.errors").html("");
<% if @event_category.errors.any? %>
  <% @event_category.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".event-categories").html("<%= escape_javascript(render 'index') %>");
  $("#event-category-modal").modal("hide");
 
<% end %>